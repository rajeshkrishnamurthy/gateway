package main

import "time"

const version = "0.1.0"

const proxyTimeout = 8 * time.Second

const (
	navOverview      = "overview"
	navSMS           = "sms"
	navPush          = "push"
	navHAProxy       = "haproxy"
	navCommandCenter = "command-center"
)
