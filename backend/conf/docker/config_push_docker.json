{
  # Any line can be commented with a hash.
  # Requires PUSH_FCM_BEARER_TOKEN in the environment for local dev.
  "pushProvider": "fcm",
  "pushProviderUrl": "https://fcm.googleapis.com/v1/projects/enc-scb/messages:send",
  "pushProviderConnectTimeoutSeconds": 2,
  "pushProviderTimeoutSeconds": 30,
  "grafanaDashboardUrl": "http://localhost:3000/d/gateway-overview-push?kiosk=tv"
}
