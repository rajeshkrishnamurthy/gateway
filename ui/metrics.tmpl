<main class="page">
  <header class="page-header">
    <div class="brand">
      <h1>Gateway Overview</h1>
      <p>Grafana dashboard embedded for live visibility.</p>
    </div>
    {{template "nav.tmpl" .}}
  </header>

  <section class="panel">
    <h2>Grafana dashboard</h2>
    <iframe class="grafana-frame" src="{{.MetricsURL}}" title="Gateway Overview dashboard" loading="lazy"></iframe>
    <p class="muted">If the dashboard does not load, open <a href="{{.MetricsURL}}">the Grafana dashboard</a>.</p>
  </section>
</main>
