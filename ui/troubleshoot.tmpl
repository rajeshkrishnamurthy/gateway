<main class="page">
  <header class="page-header">
    <div class="brand">
      <h1>Troubleshoot by ReferenceId</h1>
      <p>Logs are the source of truth for submission outcomes. Results are bounded and best-effort.</p>
    </div>
    {{template "nav.tmpl" .}}
  </header>

  <section class="panel">
    <h2>Filter logs</h2>
    <form class="form-grid" method="post" action="/ui/troubleshoot" hx-post="/ui/troubleshoot" hx-target="#troubleshoot-results" hx-swap="innerHTML">
      <div class="field">
        <label for="referenceId">referenceId</label>
        <input id="referenceId" name="referenceId" type="text" required />
      </div>
      <div class="actions">
        <button class="button" type="submit">View logs</button>
      </div>
    </form>
    <div class="note">
      <ul>
        <li>Logs are bounded and best-effort.</li>
        <li>No pagination or free-text search.</li>
        <li>No guarantees of completeness.</li>
        <li>No correlation across referenceIds.</li>
        <li>No attempt to infer delivery or retry eligibility.</li>
      </ul>
    </div>
  </section>

  <section class="panel" id="troubleshoot-results">
    <h2>Log results</h2>
    <p class="muted">Enter a referenceId to view recent log lines.</p>
  </section>
</main>
