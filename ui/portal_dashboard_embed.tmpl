<main class="page">
  <header class="page-header">
    <div class="brand">
      <h1>{{.Title}}</h1>
    </div>
  </header>

  <section class="panel">
    <iframe class="grafana-frame" src="{{.DashboardURL}}" title="{{.Title}}" loading="lazy"></iframe>
  </section>
</main>
